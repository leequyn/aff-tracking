name: Apply remaining videos patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-remaining-videos-patch.yml'
      - 'scripts/apply_remaining_videos_patch.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/apply_remaining_videos_patch.py
      - name: Commit patched files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html supabase_remaining_videos.sql
          git rm -f scripts/apply_remaining_videos_patch.py .github/workflows/apply-remaining-videos-patch.yml
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Add remaining video tracking"
          git push
